version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    preTest:
      commands:
        - npm run test
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
